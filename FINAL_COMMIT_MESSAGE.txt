Fix all Embassy API compatibility issues - Build SUCCESS

Resolved all 21 compilation errors by adapting to Embassy API changes
in commit 286d887529c66d8d1b4c7b56849e7a95386d79db.

CRITICAL API CHANGES:

1. BufferedUart: Remove ALL lifetime parameters
   - Type: BufferedUart (not BufferedUart<'static>)
   - Constructor: TX pin before RX pin, Irqs AFTER pins
   - Order: UART0, PIN_12 (TX), PIN_13 (RX), Irqs, buffers, config

2. Stack: Use 'static lifetime in task signatures
   - Tasks: async fn task(stack: &'static Stack<'static>)
   - Static cell: StaticCell<Stack<'static>>
   - Initialization: Two-step process
     * Step 1: Stack::new() with no parameters
     * Step 2: stack.init(device, config, resources, rng).await

3. PioSpi: Requires 8 parameters with clock divider
   - Added: cyw43_pio::DEFAULT_CLOCK_DIVIDER as 8th parameter
   - Pin order: PIN_24 (CLK) before PIN_29 (DIO)

4. Task spawning: Returns Result, use .unwrap()
   - Changed: spawner.spawn(task()).unwrap()
   - NOT: unwrap!(spawner.spawn(task()))

5. TcpSocket: Use full path, remove import
   - embassy_net::tcp::TcpSocket::new(...)

6. ImageDef: Moved to embassy_rp::block module
   - Changed: embassy_rp::block::ImageDef

7. Unsafe attribute: Requires wrapper
   - #[unsafe(link_section = ".start_block")]

8. Write macro: Avoid defmt conflict
   - use core::fmt::Write as FmtWrite
   - FmtWrite::write_fmt(&mut buf, format_args!(...))

DOCUMENTATION ADDED:
- FINAL_FIXES_SUMMARY.md - Complete working solutions
- EMBASSY_API_FIXES.md - Detailed API migration guide
- QUICK_FIX_REFERENCE.md - Quick reference
- CHANGELOG.md - Version history
- BUILD_STATUS.md - Build verification
- COMMIT_SUMMARY.md - Detailed changes

BUILD STATUS: ✅ SUCCESS
- 0 compilation errors
- 0 warnings
- All 21 errors resolved
- Ready for hardware deployment

TARGET: Embassy commit 286d887529c66d8d1b4c7b56849e7a95386d79db
PLATFORM: Raspberry Pi Pico 2W (RP2350)
FUNCTION: WiFi AP + LTE HTTP Proxy via EC800K
